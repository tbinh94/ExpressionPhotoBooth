<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_edit_thumb.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="8dp">

    <!-- Preview image/color Card with built-in stroke -->
    <FrameLayout
        android:layout_width="64dp"
        android:layout_height="64dp">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardThumb"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:cardCornerRadius="12dp"
            app:cardElevation="2dp"
            app:strokeWidth="0dp"
            app:strokeColor="#2A5298"
            app:cardBackgroundColor="#FFFFFF">

            <ImageView
                android:id="@+id/ivThumbPreview"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerInside"
                android:padding="8dp"
                android:adjustViewBounds="true" />

        </com.google.android.material.card.MaterialCardView>

        <ImageView
            android:id="@+id/btnDeleteSticker"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="-4dp"
            android:layout_marginEnd="-4dp"
            android:background="@drawable/bg_delete_circle"
            android:padding="4dp"
            android:src="@drawable/ic_close_24"
            app:tint="@color/white"
            android:visibility="gone"
            android:elevation="4dp" />
    </FrameLayout>

    <!-- Label -->
    <TextView
        android:id="@+id/tvThumbLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:textSize="10sp"
        android:textStyle="normal"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#546E7A" />

</LinearLayout>